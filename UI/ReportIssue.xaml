<Window x:Class="UI.ReportIssue"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ReportIssue" Height="300" Width="300"
        xmlns:Views="clr-namespace:UI.Views">
    <Grid>
        <Views:EditBugView DataContext="{Binding RelativeSource={RelativeSource Self}}" />
    </Grid>
</Window>
