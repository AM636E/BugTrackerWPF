<UserControl x:Class="UI.Views.ButtonPane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="73" d:DesignWidth="425"
             xmlns:ViewModel="clr-namespace:UI.ViewModel">
    <UserControl.Resources>
        <ViewModel:MainViewModel x:Key="MainViewModel"/>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Click="Button_Click">Show Issues</Button>
        <Button Grid.Column="1" Click="Button_Click_1">Report an Issue</Button>
        <Button Grid.Column="2">Show Employees</Button>
        <Button Grid.Column="3">Show Projects</Button>
    </Grid>
</UserControl>
